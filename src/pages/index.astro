---
import Layout from "../layouts/Layout.astro";
import TarotDraw from "../components/react/TarotDraw.tsx";
import { getNews } from "../library/microcms";
import { format } from "date-fns";

const response = await getNews({
	limit: 3,
	fields: ["id", "title", "publishedAt", "category", "eyecatch"],
});
const { contents: newsList } = response;

function formatDate(dateString: string) {
	return format(new Date(dateString), "yyyy.MM.dd");
}
// const newsList: any[] = [];

// Course Data Definition for Scalability
const courseCategories = [
    {
        id: "tarot",
        name: "ボイジャータロット",
        description: "Voyager Tarot",
        steps: [
            { step: 1, label: "リーダー認定" },
            { step: 2, label: "コーチ認定" },
            { step: 3, label: "ティーチャー認定" }
        ],
        cards: [
            { title: "リーダー認定講座", img: "/images/top_2-1.webp", url: "/course#course_1" },
            { title: "コーチ認定講座", img: "/images/top_2-2.webp", url: "/course#course_2" },
            { title: "ティーチャー認定講座", img: "/images/top_2-3.webp", url: "/course2#course_1" }
        ]
    },
    {
        id: "other",
        name: "（例）その他講座",
        description: "Other Courses",
        steps: [
            { step: 1, label: "入門" },
            { step: 2, label: "応用" },
            { step: 3, label: "マスター" }
        ],
        cards: [ // Using same images as placeholders for the example
            { title: "入門講座", img: "/images/top_2-1.webp", url: "#" },
            { title: "応用講座", img: "/images/top_2-2.webp", url: "#" },
            { title: "マスター講座", img: "/images/top_2-3.webp", url: "#" }
        ]
    }
];
---

<Layout title="Home" wide={true}>
	<!-- Hero Section (Immersive Full Screen) -->
	<section
		class="relative h-screen w-full flex items-center justify-center overflow-hidden"
	>
		<!-- Background Image Slideshow -->
		<div class="absolute inset-0 z-0">
			<div
				class="absolute inset-0 bg-tarot-blue/30 z-10 pointer-events-none"
			>
			</div>
			<!-- Overlay -->

			<img
				src="/images/title_1.webp"
				alt="Hero Background 1"
				class="absolute inset-0 w-full h-full object-cover transition-opacity duration-[2000ms] ease-in-out opacity-100 hero-bg"
				data-index="0"
			/>
			<img
				src="/images/title_2.webp"
				alt="Hero Background 2"
				class="absolute inset-0 w-full h-full object-cover transition-opacity duration-[2000ms] ease-in-out opacity-0 hero-bg"
				data-index="1"
			/>
            <!-- Added 3rd Image from Mission Section -->
			<img
				src="/images/top_3_bottom_photos_item-2.webp"
				alt="Hero Background 3"
				class="absolute inset-0 w-full h-full object-cover transition-opacity duration-[2000ms] ease-in-out opacity-0 hero-bg"
				data-index="2"
			/>
		</div>

		<script>
			// Crossfade for hero images (3 images)
            const heroBgs = document.querySelectorAll('.hero-bg');
            let currentIndex = 0;

            if (heroBgs.length > 0) {
                setInterval(() => {
                    const nextIndex = (currentIndex + 1) % heroBgs.length;
                    
                    heroBgs[currentIndex].classList.remove('opacity-100');
                    heroBgs[currentIndex].classList.add('opacity-0');
                    
                    heroBgs[nextIndex].classList.remove('opacity-0');
                    heroBgs[nextIndex].classList.add('opacity-100');

                    currentIndex = nextIndex;
                }, 5000);
            }
		</script>

		<!-- Hero Content -->
		<div class="relative z-20 text-center text-white px-4">
			<h1
				class="text-6xl md:text-8xl font-rounded font-bold mb-6 tracking-wide drop-shadow-lg leading-tight animate-fade-in-up delay-100"
			>
				Voyager Tarot<br />
				<span>Creations</span>
			</h1>

			<p
				class="text-lg md:text-2xl font-rounded tracking-widest opacity-90 mb-12 animate-fade-in-up delay-200"
			>
				未来を創造する力を、あなたに。
			</p>

			<div
				class="flex flex-col sm:flex-row justify-center gap-6 animate-fade-in-up delay-300"
			>
				<a
					href="#tarot-draw"
					class="bg-gradient-to-r from-sakura-vivid to-sunshine text-white px-10 py-4 rounded-full hover:scale-105 transition-all duration-300 font-bold tracking-wide shadow-lg shadow-pink-200"
				>
					今日の1枚を引く
				</a>
                <a
                    href="#course"
                    class="bg-white/10 backdrop-blur-md border border-white/50 text-white px-10 py-4 rounded-full hover:bg-white hover:text-tarot-blue transition-all duration-300 font-bold tracking-wide shadow-lg"
                >
                    講座一覧を見る
                </a>
			</div>
		</div>
	</section>

	<!-- Content Wrapper -->
	<div class="bg-bg-base relative z-10">

		<!-- 1-4. CONSOLIDATED SECTION: WHO WE ARE (Brand Story) -->
		<section class="mb-32 pt-24 px-4 relative">
			<div class="max-w-7xl mx-auto">
                <div class="scroll-animation opacity-0">
                    <!-- MAIN HEADER (Kept only this one) -->
                    <div class="mb-24 text-center">
                         <span class="text-gold-main font-bold tracking-widest text-sm mb-2 block">WHO WE ARE</span>
                        <h2 class="text-tarot-blue font-rounded font-bold text-3xl md:text-4xl inline-block relative">
                            ボイジャータロット<br class="md:hidden" />クリエイションズについて
                        </h2>
                        <div class="w-16 h-[2px] bg-gold-main mx-auto mt-6"></div>
                    </div>

                    <div class="space-y-32"> <!-- Unified vertical spacing for flow -->
                        
                        <!-- BLOCK 1: INTRO (Image Left, Text Right) -->
                        <div class="flex flex-col md:flex-row items-center gap-12 md:gap-20">
                            <!-- Image Side -->
                            <div class="w-full md:w-1/2 relative min-h-[400px] md:min-h-[500px] rounded-[3rem] overflow-hidden shadow-2xl shadow-blue-100/50 group">
                                 <div
                                    class="absolute inset-0 bg-tarot-blue/10 group-hover:bg-transparent transition-colors duration-500 z-10"
                                >
                                </div>
                                <img
                                    src="/images/top_1.webp"
                                    alt="Voyager Tarot Session"
                                    class="absolute inset-0 w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-1000"
                                />
                            </div>

                            <!-- Text Side -->
                            <div class="w-full md:w-1/2 flex flex-col justify-center">
                                <div
                                    class="space-y-8 text-gray-600 leading-loose font-rounded text-lg"
                                >
                                    <p class="text-xl md:text-2xl font-bold text-tarot-dark leading-relaxed">
                                        認定ティーチャーとして<br/>10年以上の経験を持つ<br/>
                                        <span class="text-sakura-vivid text-3xl md:text-4xl">青木咲羅</span>が主宰する団体です。
                                    </p>
                                    <p>
                                        ボイジャータロットの作者、ジェームズ・ワンレス博士の源流を正統に受け継ぎながら、現代に合わせた新しいアプローチを探求しています。
                                    </p>
                                    <p>
                                        ただ占うだけでなく、「現実を創造する」ためのツールとして。魅力的で実力あるリーダー、コーチ、ティーチャーの育成を目指して活動しています。
                                    </p>
                                </div>

                                <div class="mt-12">
                                    <a
                                        href="/profile"
                                        class="inline-flex items-center gap-3 px-8 py-3 rounded-full border border-sky text-sky font-bold tracking-widest hover:bg-sky hover:text-white transition-all group duration-300"
                                    >
                                        READ MORE
                                        <span class="transition-transform group-hover:translate-x-1">&rarr;</span>
                                    </a>
                                </div>
                            </div>
                        </div>

                        <!-- BLOCK 2: ABOUT CARD (Text Left, Image Right) -->
                         <div class="flex flex-col md:flex-row items-center gap-12 md:gap-20">
                            <!-- Text Side -->
                            <div class="w-full md:w-1/2 order-2 md:order-1">
                                <h3 class="text-3xl md:text-5xl font-bold text-tarot-dark mb-8 font-rounded leading-tight">
                                    「自分の人生を<br/>創造するためのツール」
                                </h3>
                                <p class="leading-loose text-gray-600 mb-12 font-rounded text-lg">
                                    従来の「占い」という枠を超え、現代の心理学や宇宙論を融合。<br />
                                    78枚のカードには美しいコラージュが描かれ、スピリチュアルなエネルギーと現実的なメッセージが詰まっています。
                                </p>

                                <div class="flex flex-col gap-10 font-rounded">
                                    <div class="flex gap-5">
                                        <div class="w-1 h-auto bg-gradient-to-b from-sakura-vivid to-transparent rounded-full"></div>
                                        <div>
                                            <h3 class="font-bold text-2xl text-tarot-dark mb-2">
                                                現実創造のサポート
                                            </h3>
                                            <p class="text-gray-600 leading-relaxed">
                                                カードのメッセージを通じて行動を促し、目標達成へと導きます。
                                            </p>
                                        </div>
                                    </div>
                                    <div class="flex gap-5">
                                        <div class="w-1 h-auto bg-gradient-to-b from-sunshine to-transparent rounded-full"></div>
                                        <div>
                                            <h3 class="font-bold text-2xl text-tarot-dark mb-2">
                                                コーチングに最適なツール
                                            </h3>
                                            <p class="text-gray-600 leading-relaxed">
                                                スピリチュアルな視点と現実的なアプローチが融合しており、自己発見や成長を強力にサポートします。
                                            </p>
                                        </div>
                                    </div>
                                    <div class="flex gap-5">
                                        <div class="w-1 h-auto bg-gradient-to-b from-sky to-transparent rounded-full"></div>
                                        <div>
                                            <h3 class="font-bold text-2xl text-tarot-dark mb-2">
                                                直感を引き出すデザイン
                                            </h3>
                                            <p class="text-gray-600 leading-relaxed">
                                                美しいビジュアルが潜在意識を刺激し、新しい気づきをもたらします。
                                            </p>
                                        </div>
                                    </div>
                                </div>

                                <div class="mt-16">
                                    <a href="/about" class="inline-flex items-center gap-3 px-8 py-3 rounded-full border border-sky text-sky font-bold tracking-widest hover:bg-sky hover:text-white transition-all group duration-300">
                                        READ MORE
                                        <span class="transition-transform group-hover:translate-x-1">&rarr;</span>
                                    </a>
                                </div>
                            </div>

                            <!-- Image Side -->
                            <div class="w-full md:w-1/2 relative md:h-[600px] h-[400px] order-1 md:order-2">
                                    <!-- Abstract geometric background -->
                                    <div class="absolute top-10 right-10 w-full h-full bg-gradient-to-br from-gray-100 to-white rounded-[3rem] -z-10"></div>
                                    
                                    <div class="relative w-full h-full rounded-[3rem] overflow-hidden shadow-2xl shadow-gray-200/50 group bg-gray-50 border border-white">
                                        <div class="absolute inset-0 bg-gradient-to-tr from-indigo-100/40 to-sakura-light/40 mix-blend-multiply z-10 transition-opacity duration-700 opacity-80 group-hover:opacity-0"></div>
                                        
                                        <div class="absolute inset-0 flex items-center justify-center text-gray-400 font-rounded tracking-widest z-0">
                                        <span class="z-0 relative">Voyager Image</span>
                                    </div>
                                    <div class="absolute inset-0 bg-white/10 backdrop-blur-[2px] opacity-0 group-hover:opacity-100 transition-opacity duration-700 z-20"></div>
                                </div>
                            </div>
                        </div>

                        <!-- BLOCK 3: CHANGE (Image Left, Text Right) -->
                        <div class="flex flex-col md:flex-row items-stretch gap-12 md:gap-20">
                            <!-- Image Side -->
                            <div class="w-full md:w-1/2 relative min-h-[400px] order-1 md:order-1 group rounded-[3rem] overflow-hidden shadow-2xl shadow-sakura-light/30">
                                    <div
                                    class="absolute inset-0 bg-tarot-blue/5 group-hover:bg-transparent transition-colors duration-500 z-10"
                                >
                                </div>
                                <img
                                    src="/images/top_5.webp"
                                    alt="Change"
                                    class="absolute inset-0 w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-700"
                                />
                            </div>

                            <!-- Text Side -->
                            <div class="w-full md:w-1/2 order-2 md:order-2 py-8">
                                <h3 class="text-3xl md:text-5xl font-rounded text-tarot-dark font-bold mb-8 leading-tight">
                                    ボイジャータロットが</br>もたらす変化
                                </h3>
                                <p class="leading-loose text-gray-600 mb-12 font-rounded text-lg">
                                    ボイジャータロットを使うことで、人生に次のような変化が訪れます
                                </p>
                                <ul class="space-y-10 font-rounded">
                                    <li class="flex gap-5">
                                        <div class="w-1 h-auto bg-gradient-to-b from-sakura-vivid to-transparent rounded-full"></div>
                                        <div>
                                            <h3 class="font-bold text-2xl text-tarot-dark mb-2">
                                                内面のクリアリング
                                            </h3>
                                            <p class="text-gray-600 leading-relaxed">
                                                自分の本音を知り、迷いや不安を解消。
                                            </p>
                                        </div>
                                    </li>
                                    <li class="flex gap-5">
                                        <div class="w-1 h-auto bg-gradient-to-b from-sunshine to-transparent rounded-full"></div>
                                        <div>
                                            <h3 class="font-bold text-2xl text-tarot-dark mb-2">
                                                具体的な行動へのヒント
                                            </h3>
                                            <p class="text-gray-600 leading-relaxed">
                                                次の一歩が明確になり、行動するモチベーションが高まります。
                                            </p>
                                        </div>
                                    </li>
                                    <li class="flex gap-5">
                                        <div class="w-1 h-auto bg-gradient-to-b from-sky to-transparent rounded-full"></div>
                                        <div>
                                            <h3 class="font-bold text-2xl text-tarot-dark mb-2">
                                                意識の拡大
                                            </h3>
                                            <p class="text-gray-600 leading-relaxed">
                                                直感を刺激され、新しい視点に気づけるようになります。
                                            </p>
                                        </div>
                                    </li>
                                    <li class="flex gap-5">
                                        <div class="w-1 h-auto bg-gradient-to-b from-mint to-transparent rounded-full"></div>
                                        <div>
                                            <h3 class="font-bold text-2xl text-tarot-dark mb-2">
                                                現実創造の加速
                                            </h3>
                                            <p class="text-gray-600 leading-relaxed">
                                                行動が変わることで、目標達成や自己実現がスピーディに進みます。
                                            </p>
                                        </div>
                                    </li>
                                </ul>
                            </div>
                        </div>

                        <!-- BLOCK 4: MISSION (Text Left, Image Right) -->
                        <div class="flex flex-col md:flex-row items-center gap-16">
                            <!-- Text Side -->
                            <div class="w-full md:w-1/2 text-left order-2 md:order-1">
                                <h3 class="text-3xl md:text-5xl font-rounded text-tarot-dark font-bold mb-8 leading-tight">
                                    未来を創造する力を、<br/>あなたに。
                                </h3>
                                
                                <div class="space-y-8 text-gray-600 leading-loose font-rounded text-lg">
                                    <p>
                                        ボイジャータロットを通じて、<br/>皆さまの成長と可能性を広げること。
                                    </p>
                                    <p>
                                        創造性・成長・実現化をテーマに、魅力的、かつ実力あるボイジャータロットリーダー、コーチ、ティーチャーの育成に取り組んでまいります。
                                    </p>
                                </div>
                            </div>

                            <!-- Image Side -->
                            <div class="w-full md:w-1/2 relative min-h-[400px] order-1 md:order-2 group rounded-[3rem] overflow-hidden shadow-2xl shadow-gold-light/50">
                                    <div
                                    class="absolute inset-0 bg-gold-light/10 group-hover:bg-transparent transition-colors duration-500 z-10"
                                >
                                </div>
                                <img
                                    src="/images/top_3_bottom_photos_item-1.webp"
                                    alt="Mission Photo"
                                    class="absolute inset-0 w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-500"
                                />
                            </div>
                        </div>

                    </div>
                </div>
            </div>
		</section>

		<!-- 5. COURSE (Scalable Sidebar Layout) -->
		<section id="course" class="mb-32 px-4 py-24 relative scroll-mt-32 bg-gray-50 rounded-[3rem]">
			<div class="max-w-7xl mx-auto">
            <div class="scroll-animation opacity-0">
                 <div class="mb-16 text-center">
                    <span class="text-gold-main font-bold tracking-widest text-sm mb-2 block">COURSE</span>
                    <h2 class="text-tarot-blue font-rounded font-bold text-3xl md:text-4xl">
                        講座紹介
                    </h2>
                    <div class="w-16 h-[2px] bg-gold-main mx-auto mt-6"></div>
                </div>

                <div class="grid md:grid-cols-12 gap-12">
                    <!-- Left Sidebar (Title & Tabs) -->
                    <div class="md:col-span-3">
                        <div class="">                            
                            <!-- Vertical Tabs -->
                            <div class="flex flex-col gap-4">
                                {courseCategories.map((cat, index) => (
                                    <button
                                        class:list={[
                                            "course-tab text-left px-6 py-4 rounded-xl transition-all duration-300 font-bold tracking-wide flex items-center justify-between group",
                                            index === 0 
                                                ? "bg-tarot-blue text-white shadow-xl shadow-blue-200" 
                                                : "bg-white text-gray-500 hover:text-tarot-blue hover:bg-blue-50"
                                        ]}
                                        data-target={`course-content-${cat.id}`}
                                    >
                                        <span class="z-10 relative">{cat.name}</span>
                                        <span class:list={["w-2 h-2 rounded-full transition-all", index === 0 ? "bg-sakura-vivid" : "bg-gray-300 group-hover:bg-tarot-blue"]}></span>
                                    </button>
                                ))}
                            </div>
                        </div>
                    </div>

                    <!-- Right Content (Steps & Cards) -->
                    <div class="md:col-span-9">
                    {courseCategories.map((cat, index) => (
                        <div 
                            id={`course-content-${cat.id}`} 
                            class:list={["course-content transition-all duration-500", index !== 0 ? "hidden" : ""]}
                        >
                            <!-- Steps Visualization -->
                            <div class="mb-20">
                                <h3 class="text-center font-bold text-gray-400 text-sm tracking-widest mb-10 opacity-70">- STEP UP FLOW -</h3>
                                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-tarot-dark font-bold font-rounded">
                                    {cat.steps.map((step, i) => {
                                        const stepColors = ["bg-sakura-vivid", "bg-sunshine", "bg-sky"];
                                        const colorClass = stepColors[i % stepColors.length];
                                        return (
                                        <div class="relative flex flex-col items-center justify-center">
                                            <div class="bg-white px-8 py-5 rounded-2xl border border-blue-50 shadow-md flex items-center justify-center gap-4 w-full relative z-10 hover:shadow-lg transition-all duration-300">
                                                <span class={`w-8 h-8 ${colorClass} text-white rounded-full flex items-center justify-center text-sm font-sans shadow-sm shrink-0`}>
                                                    {step.step}
                                                </span>
                                                <span class="whitespace-nowrap text-lg">{step.label}</span>
                                            </div>
                                            
                                            {i < cat.steps.length - 1 && (
                                                <div class="absolute z-20 text-gray-300
                                                    md:top-1/2 md:left-full md:ml-3 md:-translate-x-1/2 md:-translate-y-1/2 md:bottom-auto md:right-auto md:mt-0
                                                    top-full left-1/2 mt-3 -translate-x-1/2 -translate-y-1/2
                                                ">
                                                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" class="rotate-90 md:rotate-0">
                                                        <path d="m9 18 6-6-6-6"></path>
                                                    </svg>
                                                </div>
                                            )}
                                        </div>
                                    )})}
                                </div>
                            </div>
                            <!-- Course Cards Grid -->
                            <div class="grid md:grid-cols-3 gap-8">
                                {cat.cards.map((card) => (
                                    <a href={card.url} class="group block h-full">
                                        <div class="overflow-hidden rounded-3xl mb-5 shadow-lg shadow-gray-100 relative aspect-[4/3]">
                                            <div class="absolute inset-0 bg-tarot-blue/0 group-hover:bg-tarot-blue/10 transition-colors z-10"></div>
                                            <img
                                                src={card.img}
                                                alt={card.title}
                                                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700"
                                            />
                                        </div>
                                        <h3 class="font-bold text-center text-lg text-tarot-dark group-hover:text-sakura-vivid transition-colors mb-2">
                                            {card.title}
                                        </h3>
                                        <div class="text-center text-sm text-gray-400 flex justify-center items-center gap-1 group-hover:gap-2 transition-all">
                                            詳細はこちら 
                                            <span class="text-sakura-vivid opacity-0 group-hover:opacity-100 transition-opacity">&rarr;</span>
                                        </div>
                                    </a>
                                ))}
                            </div>
                        </div>
                    ))}
                </div>
            </div>
        </div>
        </div>
		</section>

        <script>
            // Tab Switching Logic
            const tabs = document.querySelectorAll('.course-tab');
            const contents = document.querySelectorAll('.course-content');

            tabs.forEach(tab => {
                tab.addEventListener('click', () => {
                    const targetId = (tab as HTMLElement).dataset.target;
                    if (!targetId) return;

                    // 1. Reset Tabs
                    tabs.forEach(t => {
                        t.classList.remove('bg-tarot-blue', 'text-white', 'shadow-xl', 'shadow-blue-200');
                        t.classList.add('bg-white', 'text-gray-500');
                         // reset marker
                         const marker = t.querySelector('span:last-child');
                         if(marker) {
                             marker.classList.remove('bg-sakura-vivid');
                             marker.classList.add('bg-gray-300');
                         }
                    });

                    // 2. Active Tab
                    tab.classList.remove('bg-white', 'text-gray-500');
                    tab.classList.add('bg-tarot-blue', 'text-white', 'shadow-xl', 'shadow-blue-200');
                     const activeMarker = tab.querySelector('span:last-child');
                         if(activeMarker) {
                             activeMarker.classList.remove('bg-gray-300');
                             activeMarker.classList.add('bg-sakura-vivid');
                         }

                    // 3. Switch Content
                    contents.forEach(content => {
                        content.classList.add('hidden');
                        content.classList.remove('animate-fade-in-up');
                    });
                    
                    const targetContent = document.getElementById(targetId);
                    if (targetContent) {
                        targetContent.classList.remove('hidden');
                         targetContent.classList.add('animate-fade-in-up');
                    }
                });
            });

            // Scroll Reveal Animation (Draw Card Effect)
            const observerOptions = {
                root: null,
                rootMargin: '0px',
                threshold: 0.15
            };

            const observer = new IntersectionObserver((entries, observer) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.remove('opacity-0');
                        entry.target.classList.add('animate-draw-card');
                        observer.unobserve(entry.target); // Draw once per session
                    }
                });
            }, observerOptions);

            document.querySelectorAll('.scroll-animation').forEach(el => {
                observer.observe(el);
            });
        </script>

	<!-- 6. NEWS -->
	<section class="mb-32 px-4 relative">
		<div class="max-w-7xl mx-auto">
        <div class="scroll-animation opacity-0">
             <div class="mb-16 text-center">
                 <span class="text-gold-main font-bold tracking-widest text-sm mb-2 block">LATEST NEWS</span>
                <h2 class="text-tarot-blue font-rounded font-bold text-3xl md:text-4xl">
                    お知らせ
                </h2>
                <div class="w-16 h-[2px] bg-gold-main mx-auto mt-6"></div>
            </div>

            <div class="grid gap-10 md:grid-cols-3 mb-12">
                {
                    newsList.map((news) => (
                        <a
                            href={`/news/${news.id}`}
                            class="block bg-white rounded-3xl shadow-sm border border-gray-100 hover:shadow-xl hover:shadow-blue-100/50 hover:-translate-y-2 transition-all duration-500 group overflow-hidden relative"
                        >
                            <div class="absolute inset-0 bg-gradient-to-br from-white via-transparent to-sakura-light/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
                            <div class="h-64 bg-gray-50 mb-0 overflow-hidden relative">
                                {news.eyecatch ? (
                                    <img
                                        src={news.eyecatch.url}
                                        alt={news.title}
                                        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"
                                    />
                                ) : (
                                    <div class="w-full h-full flex items-center justify-center bg-gray-50 text-gray-300">
                                        <span class="text-sm tracking-widest">
                                            Voyager
                                        </span>
                                    </div>
                                )}
                                <div class="absolute top-4 left-4">
                                    <span class="bg-white/90 backdrop-blur text-xs text-tarot-dark font-bold px-3 py-1 rounded-full shadow-sm">
                                        {news.category[0]?.name}
                                    </span>
                                </div>
                            </div>
                            <div class="p-8 relative z-10">
                                <time class="text-xs text-gray-400 font-bold tracking-wider block mb-3">
                                    {formatDate(news.publishedAt)}
                                </time>
                                <h3 class="font-bold text-xl line-clamp-2 text-text-main group-hover:text-tarot-blue transition-colors leading-relaxed mb-4">
                                    {news.title}
                                </h3>
                                <div class="flex justify-end">
                                    <span class="text-sakura-vivid text-sm font-bold opacity-0 group-hover:opacity-100 transform translate-x-[-10px] group-hover:translate-x-0 transition-all duration-300">
                                        Read More &rarr;
                                    </span>
                                </div>
                            </div>
                        </a>
                    ))
                }
            </div>

            <div class="text-center">
                <a
                    href="/news"
                    class="inline-flex items-center gap-3 px-8 py-3 rounded-full border border-gray-300 text-gray-500 font-bold tracking-widest hover:border-tarot-blue hover:text-tarot-blue transition-all group duration-300"
                >
                    VIEW ALL
                    <span class="transition-transform group-hover:translate-x-1">&rarr;</span>
                </a>
            </div>
        </div>
		</div>
	</section>

	<!-- 7. TAROT DRAW -->
	<section
		id="tarot-draw"
		class="mb-20 py-16 px-4 bg-gradient-to-br from-white to-blue-50 text-text-main rounded-[3rem] text-center relative overflow-hidden shadow-glass border border-white"
	>
		<div class="relative z-10">
            <span class="text-gold-main font-bold tracking-widest text-sm mb-4 block">TODAY'S TAROT</span>
			<h2 class="text-4xl md:text-6xl font-rounded mb-8 font-bold text-tarot-blue">
                今日の1枚を引く
            </h2>
            <p class="text-gray-600 mb-8 font-rounded">
                タロットからメッセージを受け取りましょう。
            </p>
			<TarotDraw client:visible />
		</div>
		<!-- Decorative bg elements -->
		<div
			class="absolute top-0 right-0 w-96 h-96 bg-gradient-to-b from-sky/10 to-transparent rounded-full blur-3xl -z-0 opacity-60"
		>
		</div>
        <div
			class="absolute bottom-0 left-0 w-96 h-96 bg-gradient-to-t from-sakura-light/20 to-transparent rounded-full blur-3xl -z-0 opacity-60"
		>
		</div>
	</section>
    <!-- Closing the main content wrapper -->
	</div>
</Layout>
